import {
  "MaoDingA/shrimp_cards/client" @client,
  "MaoDingA/shrimp_cards/protocol" @protocol,
  "MaoDingA/shrimp_cards/transport/websocket" @websocket,
  "moonbitlang/async/websocket" @raw_ws,
}

import {
  "moonbitlang/async" @async,
  "moonbitlang/async/http" @http,
  "moonbitlang/async/socket" @socket,
} for "wbtest"

supported_targets = "+native"
